対象職種：
<% occupation_list.each do |tag| %>
<%= link_to posts_path(tag_name: tag), class: "badge badge-pill badge-success", data: {"turbolinks"=>false} do %>
<i class="fas fa-tags"></i><%= tag %>
<% end %>
<% end %>