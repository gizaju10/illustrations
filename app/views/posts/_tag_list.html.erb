
カテゴリー：
<% tag_list.each do |tag| %>
<%= link_to tag, posts_path(tag_name: tag), class: "" %>
<% end %>


対象職種：
<% Post.find_by(id: 2).occupation_list.each do |tag| %>
<%= link_to tag, posts_path(tag_name: tag), class: "" %>
<% end %>