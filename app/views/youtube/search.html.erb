<center>
<h4><i class="fab fa-youtube"></i> 検索して投稿</h4>
 <%# <div class="col-md-3 col-md-offset-3 search-form"> %>
     <%= form_tag( youtube_search_path,:method => 'get') do %>
     <%# <label for="keyword">検索</label> %>
     <%= text_field_tag :keyword, '', class: '', placeholder: "YouTubeから投稿する" %>
             <%= button_tag :name => nil, :class =>"btn-danger badge-light" do %>
        <i class="fas fa-file-video"></i>検索
        <% end %>
     <% end %>
 <%# </div> %>
</center>
<% if request.fullpath =~ /\?keyword\=/ %>
 <% @movies.each do |movie| %>
 <hr>


<div class="movies-item">

    <div class="col-md-6 mb-md-0 p-md-4">
        <div class="max-youtube">
            <div class="responsibe-youtube">
                <iframe width="560" height="315" src="https://www.youtube.com/embed/<%= movie[:id][:video_id] %>"?rel=0"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen></iframe>
            </div>
        </div>
    </div>










 

 </div>
 <dl class="p-faq">
     <dt class="p-faq-title">投稿する</dt>

     <dd class="p-faq-description">
                 <%# <hr> %>
<%= form_for @post do |f| %>
<%# <%= f.label :content %>
<% @post.errors.full_messages_for(:title).each do |message| %>
<div class="badge badge-warning">
  <%= message %>
</div>
<% end %>
<diV>
  <%= f.text_field :title, placeholder: "タイトル" %>
</div>
<%# <hr> %>
<div class="font-weight-bold field">紹介したいYouTubeのURL:</div>
  <% @post.errors.full_messages_for(:url).each do |message| %>
  <div class="badge badge-warning">
    <%= message %>
  </div>
  <% end %>
  <diV>
    <%= f.text_field :url, placeholder: "https://www.youtube.com/watch?v=", value: "https://www.youtube.com/watch?v=#{movie[:id][:video_id]}", class: "url_text" %>
  </div>
 

<%# <hr> %>

    <%# <%= f.label :tag_list %>
    <%# <%= f.text_field :tag_list, value: @post.tag_list.join(','), class: "form-control" %>
    <div class="font-weight-bold field">カテゴリー:</div>
  <% @post.errors.full_messages_for(:category_list).each do |message| %>
  <div class="badge badge-warning">
    <%= message %>
  </div>
  <% end %>
    
    <div class="col-sm-12">
      <% @categories.each do |tag| %>
      <%= f.check_box :category_list, { multiple: true }, "#{tag.name}", nil %>
      <%= f.label "#{tag.name}(#{tag.taggings_count})", class: "badge badge-primary" %>
      <% end %>
    </div>
<%# <hr> %>
<div class="font-weight-bold field">対象職種:</div>
  <% @post.errors.full_messages_for(:occupation_list).each do |message| %>
  <div class="badge badge-warning">
    <%= message %>
  </div>
  <% end %>
    
    <div class="col-sm-12">
      <% @occupations.each do |tag| %>
      <%= f.check_box :occupation_list, { multiple: true }, "#{tag.name}", nil %>
      <%= f.label "#{tag.name}(#{tag.taggings_count})", class: "badge badge-success" %>
      <% end %>
    </div>
<%# <hr> %>
<div class="font-weight-bold field">対象者:</div>
  <% @post.errors.full_messages_for(:target_list).each do |message| %>
  <div class="badge badge-warning">
    <%= message %>
  </div>
  <% end %>
    <%# <label class="col-sm-12">対象者</label> %>
    
    <div class="col-sm-12">
      <% @targets.each do |tag| %>
      <%= f.check_box :target_list, { multiple: true }, "#{tag.name}", nil %>
      <%= f.label "#{tag.name}(#{tag.taggings_count})", class: "badge badge-danger" %>
      <% end %>
    </div>

<%# <hr> %>
    <% @post.errors.full_messages_for(:content).each do |message| %>
    <div class="badge badge-warning">
      <%= message %>
    </div>
    <% end %>
    <diV class=" field">
      <%= f.text_area :content, placeholder: "紹介文" %>
    </div>
    <%= f.submit '投稿', class: 'btn btn-primary' %>
    <% end %>
     </dd>
     <p>
         <% end %>
         <%= link_to("YouTubeで確認する", "https://www.youtube.com/", class: 'navbar-brand underscore') %>

<% else %>
<% end %>
         <script>
             $('.p-faq-title').on('click', function () {
                 $(this).toggleClass('active');
                 $(this).next().slideToggle();
             });
         </script>